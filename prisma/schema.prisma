generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider             = "mysql"
  url                  = env("mysql://doadmin:AVNS_EpQDHX4i5Zc2L28@db-mysql-blr1-88263-do-user-9189119-0.b.db.ondigitalocean.com:25060/defaultdb?ssl-mode=REQUIRED")
  referentialIntegrity = "prisma"
}

model ShortLink {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  url       String   @db.VarChar(3000)
  slug      String   @unique

  @@index([slug])
}
